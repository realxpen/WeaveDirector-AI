# WeaveDirector Frontend

Next.js App Router frontend for WeaveDirector Campaign Studio.

## Run locally

1. Install dependencies:
   `npm install`
2. Create `.env.local` and set:
   `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000`
3. Start dev server:
   `npm run dev`

